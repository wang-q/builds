# Detect text files automatically
* text=auto
# Force Unix-style line endings on these files
* eol=lf
tar/*.tar.gz filter=lfs diff=lfs merge=lfs -text
src/*.tar.gz filter=lfs diff=lfs merge=lfs -text
